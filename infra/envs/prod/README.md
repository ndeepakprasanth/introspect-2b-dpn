# Prod environment (not used)

This folder exists for future work but is intentionally left empty in this minimal scaffold.

Current workflow maintains a single working environment: `infra/envs/dev`. If you want a `prod` environment, replicate `dev` files and update settings accordingly.
